import { NumberAmilliPipe } from './number-amilli.pipe';

describe('NumberAmilliPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberAmilliPipe();
    expect(pipe).toBeTruthy();
  });
});
