import { TimetoHrminPipe } from './timeto-hrmin.pipe';

describe('TimetoHrminPipe', () => {
  it('create an instance', () => {
    const pipe = new TimetoHrminPipe();
    expect(pipe).toBeTruthy();
  });
});
